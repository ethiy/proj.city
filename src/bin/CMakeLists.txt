set(orthoproject_SRC "${proj.city_SOURCE_DIR}/src/bin/orthoproject.cpp")
add_executable(orthoproject ${orthoproject_SRC})
target_link_libraries(orthoproject urban)
set(extract_buildings_SRC "${proj.city_SOURCE_DIR}/src/bin/extract_buildings.cpp")
add_executable(extract_buildings ${extract_buildings_SRC})
target_link_libraries(extract_buildings urban)
